const steps = [['01', 'Tell us what you need', 'Share the type of care, location, and time that works best for you.'], ['02', 'Choose your nurse', 'Browse qualified professionals and find a nurse who feels right for you.'], ['03', 'Receive care at home', 'Book with confidence and receive attentive care in familiar surroundings.']]

export function HowItWorksSection() { return <section className="how-it-works" id="how-it-works"><div className="container"><span className="eyebrow">Simple by design</span><h2 className="section-heading">Care in three easy steps</h2><div className="steps">{steps.map(([number, title, description]) => <article className="step" key={number}><div className="step-number">{number}</div><h3>{title}</h3><p>{description}</p></article>)}</div></div></section> }
